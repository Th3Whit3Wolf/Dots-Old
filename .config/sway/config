# config.d/bg.conf now sets the background
# This allows me to modify the file from the command line

# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

# --no-startup-id
font pango:'Fira Mono 14'
include /etc/sway/config.d/*
include config.d/*

output * bg $wallpaper fill
set $lock swaylock -c 333333 -s fill -i $wallpaper -l --ring-color 5d4d7a

bindsym Mod1+Shift+l exec $lock #~/Pics/what_space_really_looks_like.png
#bindsym Mod1+Shift+l exec /home/doc/Gits/swaylock-blur/target/release/swaylock-blur
#bindsym Mod1+Shift+l exec swaylock-fancy -p